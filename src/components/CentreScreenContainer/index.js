import React from "react";

import "./style.css";

const CentreScreenContainer = props => {
  return <div className="centre-screen-container">{props.children}</div>;
};

export default CentreScreenContainer;
